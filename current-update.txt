가이드 페이지 작업 내역

요청한 내용대로 메뉴에 한글 가이드 페이지를 추가했고,
서비스 기능 목록과 사용 방법 안내까지 정리했습니다.

적용한 변경 사항:
- 가이드 페이지 추가: app/src/pages/GuidePage.tsx
  - 한글 우선 콘텐츠(영문 fallback 포함)
  - 주요 기능(대시보드/오버레이/투데이/상세/설정/로그인)
  - 사용 방법(단계별 안내)
  - 운영 팁 섹션

- 메뉴/라우트 연결: app/src/App.tsx
  - 상단 메뉴에 가이드 항목 추가
  - /guide 라우트 추가
  - /guide/ 접속 시 /guide 리다이렉트 처리
  - 대시보드 메뉴 active 범위 보정(end 적용)

- 스타일 추가: app/src/App.css
  - 기존 d2-ui 톤에 맞는 가이드 섹션/리스트 스타일 반영

검증 결과:
- npm --prefix app run lint 통과
- npm --prefix app run build 통과

비고:
- 기존과 동일하게 폰트 runtime-resolve 경고 및 500KB+ 번들 경고는 표시되지만,
  이번 변경으로 인한 빌드 실패나 린트 오류는 없습니다.

--------------------------------------------------
추가 작업 로그 (2026-03-12 11:08:36 +09:00)

- 확인 결과: `GuidePage`는 이미 라우트에 연결되어 있었음 (`app/src/App.tsx`).
- UI 노출 강화: 대시보드에 `사용자 가이드 열기` 바로가기 버튼 추가.
  - 반영 파일: `app/src/pages/DashboardPage.tsx`
  - 동작: 버튼 클릭 시 `/guide`로 이동
- 기존 메뉴/라우트 연결은 유지:
  - 메뉴 탭: `app/src/App.tsx`
  - 라우트: `/guide`, `/guide/` 리다이렉트
- 검증 결과:
  - `npm --prefix app run lint` 통과
  - `npm --prefix app run build` 통과
- 안내:
  - 화면에 바로 반영이 안 보이면 이전 빌드가 실행 중일 수 있으므로 앱 재시작(데스크톱 실행본은 재빌드 후 재실행) 필요.
